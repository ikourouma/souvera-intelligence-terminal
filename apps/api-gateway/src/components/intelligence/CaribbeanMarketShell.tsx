/**
 * Re-export archived v1 list shell for reference routes only.
 * Production Caribbean workspace uses CaribbeanMapPanel — see SouveraMapWorkspace.
 */
export {
  CaribbeanMarketShell,
  type CaribbeanMarketShellProps,
} from './archive/CaribbeanMarketShell.v1-list';
